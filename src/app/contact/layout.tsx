export const metadata = {
  title: "Contact — The Visual Archive",
  description: "Get in touch with The Visual Archive team.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
